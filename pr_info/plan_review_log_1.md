# review-plan review log 1

## Round 1 — 2026-08-02
**Findings**:
I'll gather context in parallel: knowledge base files, the issue, and the plan files.Let me read the rest of the issue and the plan files.Now let me read all 14 step files.Let me verify key code claims against the actual source before finalizing findings.Let me check a few more code facts: `resolve_connection` usage, `to_dialect`, count_tools, and update/query tools signatures.Let me check `resolve_connection` and how the `database` field flows to the backend, plus `to_dialect`.Let me pin exact line numbers for the findings.Review complete. Findings below (review-only; no task list was given, so no plan files were modified).

pr_info/steps/step_3.md:15 — medium — Normalising the legacy `database` field "away" (emptied) while `resolve_connection` returns the `ConnectionConfig` verbatim and `mssql.py:59` reads `config.database` for the ODBC `Database=` (server migrates only in Step 6). Steps 3–5 leave an intermediate commit where MSSQL connects with an empty catalog and any loader/`resolve_connection` test asserting `.database` goes red — violates the "each step leaves checks green / one working commit" rule. Keep `database` populated (=`default_database`) through the transition, or migrate the reader in the same commit.

pr_info/steps/step_9.md:30 — medium — The `database` param default is fixed to the file-default connection's `default_database`. When a non-default `connection` is selected without an explicit `database`, `resolve_pinned` receives a database not in that connection and raises, so the common `read_tables(connection="other")` call fails. Default should be `None` so `resolve_pinned` falls back to the selected connection's `default_database` (the Step 4 semantics already support `None`).

pr_info/steps/step_9.md:44 — medium — The issue mandates a friendly call-time verdict for the union-enum mismatch ("database 'hr' is not configured for connection 'localdb'. Available: [...]"), but the plan only relies on `resolve_pinned` raising `ValueError`; `build_schema_body` (and the Step 11 `core` at step_11.md:38) call `resolve_pinned` outside any try/except, so it surfaces as an unhandled tool error rather than the required message. No step specifies catching it and rendering the verdict.

pr_info/steps/step_11.md:26 — low — Introducing `build_target_params(..., star=False)` with a `False` default omits the `"*"` fan-out sentinel; Step 10's `schema_tools` caller (`build_target_params(targets)`) must be updated to pass `star=True` or `database="*"` fan-out silently regresses. This dependency is not stated in Step 11.

pr_info/steps/step_13.md:22 — low — After Step 6 and Step 13 migrate all production callers, `resolve_connection` and `_resolve_connection_for_verify` become dead code (only tests reference them), but no step removes them. Knowledge base requires clean deletion with no legacy artifacts.
**Decisions**:
Verdict(decision='tasks', tasks=['At pr_info/steps/step_3.md:15, keep the legacy `database` field populated (set it equal to `default_database`) through the Step 3-5 transition so intermediate commits leave MSSQL connecting to a valid catalog and loader/resolve_connection tests stay green, OR migrate the mssql.py:59 reader in the same commit that empties the field.', "At pr_info/steps/step_9.md:30, change the `database` parameter default from the file-default connection's `default_database` to `None` so that a non-default `connection` selection without an explicit `database` falls back to the selected connection's `default_database` via resolve_pinned instead of raising.", 'At pr_info/steps/step_9.md:44, add a step that catches the ValueError from resolve_pinned in build_schema_body (and the Step 11 core at step_11.md:38) and renders the required friendly call-time verdict (e.g. "database \'hr\' is not configured for connection \'localdb\'. Available: [...]") instead of surfacing an unhandled tool error.', 'At pr_info/steps/step_11.md:26, update the Step 10 schema_tools caller `build_target_params(targets)` to pass `star=True` (or database="*") so the `"*"` fan-out sentinel is preserved when `build_target_params` gains the `star=False` default, and document this dependency in Step 11.', 'At pr_info/steps/step_13.md:22, add a step to delete the now-dead `resolve_connection` and `_resolve_connection_for_verify` functions after Steps 6 and 13 migrate all production callers, leaving no legacy artifacts.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-02
**Findings**:
I'll gather context in parallel: knowledge base, the issue, and the plan files.The issue is long. Let me read the rest of it and the knowledge base and summary files.Now let me read the remaining knowledge base files and all the step files.Now the step files. Let me read them all.The plan tracks the issue closely. Let me verify a few load-bearing codebase facts the plan depends on before finalizing.The list_symbols tool is under a different prefix. Let me use it correctly.Let me verify the key signatures the plan depends on: `create_backend`, `DatabaseBackend.close`, `format_rows`, `build_query_body`, `build_tool_fn`, and how SchemaTools registers builtins.The plan is thorough. Let me verify a couple of specifics that could hide real gaps: the `ConnectionConfig` backend field and existing validators, and whether `build_tool_fn` exists where the plan assumes.Let me confirm Step 1 covers all call sites of the two prerequisite functions — a missed caller would leave the bug partially unfixed.Let me check how the existing `resolve_connection` handles an empty/unset `connection` — Step 4's `default` computation depends on `query_config.connection` naming a real connection.Let me get accurate line numbers for the findings.Review complete. The plan tracks issue #44's decisions closely and is internally consistent; the prerequisite-fix call sites, `resolve_connection` lifecycle, backend/registry APIs, and tool-surface conditionals all check out against the codebase. Only minor findings:

pr_info/steps/step_3.md:53 — low — After-validator special-cases only `mssql` and `postgresql`, but `create_backend` treats `pyodbc` as MSSQL; a `pyodbc` connection with neither `database` nor `databases` bypasses the "databases required" rule and yields a connection with zero resolved targets.
pr_info/steps/step_4.md:55 — low — Algorithm `default = next(t for t in targets if t.is_default)` raises `StopIteration`, not the commented `ValueError`, when the file-default `connection` is unknown; the empty-`connection` case (today rejected by `resolve_connection` with "No connection name specified") is not covered by the step's tests, so error-wording backward-compat is unverified.
pr_info/steps/step_13.md:5 — low — One commit bundles per-pair CONNECTION rework, per-target M2 rework, and deletion of `resolve_connection`/`_resolve_connection_for_verify` across 4 source + 5 test modules (highest-churn step); in tension with the "split large steps / one step = one commit" planning principle.
**Decisions**:
Verdict(decision='tasks', tasks=["At pr_info/steps/step_3.md:53, extend the after-validator's 'databases required' rule to cover the `pyodbc` backend type (which create_backend treats as MSSQL), so a pyodbc connection with neither `database` nor `databases` is rejected instead of yielding zero resolved targets.", 'At pr_info/steps/step_4.md:55, replace the `next(t for t in targets if t.is_default)` expression so it raises a friendly ValueError (matching the comment and the existing backward-compat error wording) instead of an uncaught StopIteration when the file-default `connection` is unknown or empty, and add tests covering both the unknown-connection and empty-connection cases to verify the error wording.'], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-08-02
**Findings**:
Now let me read the knowledge base and the plan summary.Now the step files. Let me read them all.Let me verify a few plan claims against the codebase—particularly the verify snapshot fixture and a couple of referenced symbols.Let me get line numbers for the key sections I need to cite.Let me pin down the exact line for the Step 13 test section.`pr_info/steps/step_3.md:68` — high — before-validator sets `data["database"] = data["default_database"]` and line 64 discards any authored `database` when `databases` is present, destroying the raw value before the after-validator runs; the rule-7 conflict check (step_3.md:58-59) and Step 12's static rule 7 can therefore never fire, so a contradictory `database="hr"` + `databases=["sales"]` config is silently accepted and mis-routed instead of raising.
`pr_info/steps/step_13.md:59` — low — Step 13 reworks the M2 QUERIES/UPDATES output that `tests/cli/fixtures/verify_snapshot.txt` asserts byte-for-byte, but its WHERE/TESTS omit that snapshot fixture (only Step 12, which does not touch those sections, mentions regenerating it), leaving a traceability gap if per-target row wording shifts.
**Decisions**:
Verdict(decision='tasks', tasks=['At pr_info/steps/step_3.md:68, fix the validator ordering so the rule-7 conflict check can actually fire: do not have the before-validator overwrite `database` with `default_database` or discard an authored `database` when `databases` is present before the conflict check runs. Perform the `database` + `databases` contradiction check (e.g. `database="hr"` + `databases=["sales"]`) against the raw authored values and raise, then normalize.', "At pr_info/steps/step_13.md:59, since Step 13 reworks the M2 QUERIES/UPDATES output asserted byte-for-byte by tests/cli/fixtures/verify_snapshot.txt, add the regeneration of that snapshot fixture (and verification of the new per-target row wording) to Step 13's WHERE/TESTS sections so the step leaves checks green."], escalate_reason=None)
**Changes**:
applied
