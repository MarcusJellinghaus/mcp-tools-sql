# review-plan review log 1

## Round 1 — 2026-08-26
**Findings**:
I'll gather context systematically. Starting with the knowledge base, the issue, and the plan files.The issue is truncated. Let me get the rest plus the knowledge base and plan files.Now let me verify the plan's claims against the actual codebase.`pr_info/steps/step_3.md:188` — high — `validate_where`'s signature change (`schema`/`table` → `table_ref`) breaks its only production caller, `src/mcp_tools_sql/summarize/tools.py:258` (imported at `tools.py:49`), but step 3's WHERE table omits `tools.py` and the prompt forbids touching it ("do not wire anything into `summarize/tools.py` yet"), so the step cannot leave mypy/pytest green as one commit.

`pr_info/steps/step_4.md:60` — medium — defining `label` as bare `table` on SQLite changes today's `Table main.empty_t is empty (0 rows).` to `Table empty_t is empty (0 rows).`; this contradicts the same step's "byte-identical" / "zero user-visible change" claim (lines 63, 117) and diverges from `table_not_found_message`, which keeps `schema.table` on both dialects. Existing tests only assert the substring `"is empty (0 rows)"`, so the drift is not caught.

`pr_info/steps/step_4.md:76` — high — `build_table_source` runs the metadata query but is placed outside `core`'s `try/except _INVALID_SQL_EXC / RuntimeError / Exception` tail; that query lives inside `_run` (inside the try) today, so backend failures that currently return `"Database connection error. …"` / `"Invalid SQL. …"` would escape `core` uncaught, contradicting the step's zero-behaviour-change acceptance criterion.

`pr_info/steps/step_5.md:76` — high — `build_query_source` executes the user's SELECT (probe, later DMF) outside the same `try`, so a source that parses and passes the read-only gate but fails at execution (nonexistent table, ambiguous column, type error) raises out of the tool instead of returning a message; the issue requires that "an unresolvable source surfaces as a SQL error from the DMF or the probe", and no entry in the step's TESTS list covers this path.

`pr_info/steps/step_5.md:47` — low — `_base_summarize_params()` gains `sql` and relaxes `schema`/`table`, but the plan does not update the existing `params` Field description (`"Bound values for the where :name placeholders."`) or the `schema` description (`"Owning schema (ignored on SQLite)."`), both of which become inaccurate once source placeholders bind through the same dict and `schema` is optional.

`pr_info/steps/step_6.md:75` — medium — the DMF fallback is specified to catch `_INVALID_SQL_EXC`, which is private to `src/mcp_tools_sql/summarize/tools.py:61`; `source.py` cannot import it because `tools.py` already imports `source.py` (steps 4–5). The step does not plan for moving or re-deriving that exception tuple.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_3.md, resolve the caller breakage for validate_where's signature change (schema/table -> table_ref): either add src/mcp_tools_sql/summarize/tools.py:258 (and the import at tools.py:49) to step 3's WHERE table and drop the 'do not wire anything into summarize/tools.py yet' restriction for that call site, or keep a backwards-compatible signature within step 3, so the step lands as one commit with mypy and pytest green.", "In pr_info/steps/step_4.md:76, move build_table_source's metadata query inside core's existing try/except (_INVALID_SQL_EXC / RuntimeError / Exception) tail, or otherwise specify equivalent error handling, so backend failures still return the current 'Database connection error. ...' / 'Invalid SQL. ...' messages and the zero-behaviour-change criterion holds.", "In pr_info/steps/step_5.md:76, ensure build_query_source's SELECT execution (probe and later DMF) is covered by the same try/except that maps SQL/backend failures to a returned message, and add a TESTS entry for a source that parses and passes the read-only gate but fails at execution (e.g. nonexistent table or ambiguous column), asserting a SQL error message is returned rather than an exception escaping the tool.", 'In pr_info/steps/step_6.md:75, specify how source.py obtains the _INVALID_SQL_EXC tuple given tools.py already imports source.py: plan to move the exception tuple into a shared module (or source.py itself) and have tools.py import it from there, and update the affected imports in the step.', "In pr_info/steps/step_4.md:60, reconcile the SQLite label definition with the 'byte-identical' / 'zero user-visible change' claims at lines 63 and 117: either keep the label as schema.table on SQLite to match table_not_found_message, or explicitly document the message change and add a test asserting the full empty-table message text so the behaviour is pinned."], escalate_reason=None)
**Changes**:
applied
