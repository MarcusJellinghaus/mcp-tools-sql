# review-plan review log 1

## Round 1 — 2026-08-04
**Findings**:
I'll gather context in parallel: knowledge base files, the issue, and the plan files.Let me read the rest of the issue and the plan files plus knowledge base.Now let me read the remaining knowledge base files and all step files.Now let me read all the step files.Let me examine the reference implementation `count_tools.py` and key utilities to verify the plan's claims.Let me verify referenced files and helpers exist: the roadmap doc, test helpers, `PROGRAMMATIC_BUILTIN_TOOLS`, and `execute_readonly_query` param handling.Let me check `tach.toml` and `.importlinter` to assess whether creating the package in step 1 but declaring boundaries only in step 7 leaves intermediate commits with broken architecture checks.Let me get exact line numbers for the relevant spots in the step files.Let me check how the SQLite backend's `execute_readonly_query` handles named params, since the metadata query uses `:table` inside `pragma_table_info(:table)`.Let me get the exact line for the step 5 remainder-algorithm note.Review complete. Findings below (plan-only review; no task list was provided, so no files modified).

`pr_info/steps/step_7.md:40` — medium — Empty metadata result maps to `empty_table_message`, but `INFORMATION_SCHEMA.COLUMNS`/`pragma_table_info` return zero rows for a *non-existent* table, not an empty one; emitting "Table X is empty (0 rows). Use read_columns…" for an absent table is misleading. A distinct table-not-found message is needed.
`pr_info/steps/step_6.md:36` — medium — Triage rows carry `min`/`max` keys, yet the step 3 scalar pass (`step_3.md`) computes value min/max only for numeric/temporal categories; string/boolean/other columns have no min/max, leaving the triage view's min/max unspecified (blank) for most column types. The plan should state min/max is numeric/temporal-only or add string value min/max.
`pr_info/steps/step_7.md:53` — low — `clamp_note` is appended unconditionally in pipeline step 10, but it is only produced inside the deep branch (step 9, `clamp_n(n)`); in the triage path it is unbound. Hoist `clamp_n(n)` before the view dispatch.
`pr_info/steps/step_5.md:56` — low — The `_render_values` remainder algorithm leaves an unresolved inline question ("rows here = … use total returned"); the correct denominator (total filtered `rows`) is derivable from the `customer_city` example but should be stated unambiguously rather than left open in the plan.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_7.md around line 40, distinguish a non-existent table from an empty one: zero metadata rows from INFORMATION_SCHEMA.COLUMNS/pragma_table_info means the table does not exist, so add a distinct table-not-found message instead of reusing empty_table_message ('Table X is empty (0 rows)...').", 'In pr_info/steps/step_6.md around line 36, resolve the min/max specification gap for the triage view: step 3 computes value min/max only for numeric/temporal columns, so either explicitly state that triage min/max is numeric/temporal-only (blank otherwise) or define string/boolean min/max behavior.', 'In pr_info/steps/step_7.md around line 53, fix the unbound clamp_note in the triage path: hoist clamp_n(n) before the view dispatch so clamp_note is always bound, since pipeline step 10 appends it unconditionally but step 9 only produces it in the deep branch.', 'In pr_info/steps/step_5.md around line 56, resolve the open inline question in the _render_values remainder algorithm by stating unambiguously that the denominator is the total filtered rows (as derivable from the customer_city example), rather than leaving it unspecified.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-04
**Findings**:
I'll gather context in parallel: knowledge base files, the issue, and the plan files.The issue truncated. Let me read the rest of the issue, the knowledge base, and the plan files.Now let me read the remaining knowledge base files and the plan files.Now let me read all seven step files.Let me verify the plan against the actual template code it claims to mirror.Let me verify `build_target_params` accepts `star` and check `basic_preflight` signature.Let me verify `format_rows` exists and its shape, since triage depends on it.Let me get precise line numbers for the spots I'll cite.pr_info/steps/step_7.md:57 — medium — Value-kind rule `distinct < non_null → top, else sample` is undefined for `non_null == 0`: an all-NULL column (distinct 0 == non_null 0) is classed `sample`, whose `SELECT DISTINCT` returns a lone NULL row, rendering the nonsensical `sample values (1 of 0 distinct — every value unique)`. Empty columns are a headline use case for this tool; such a column should get `value_kind == "none"`.
pr_info/steps/step_7.md:60 — medium — Orchestration passes the unclamped `n` to `render_summary`/`render_deep` while value-list SQL is built with `clamped_n` (line 55); the sample header count (`N of D distinct`, step_5.md:45) must be the actual shown count (`len(values)`), otherwise it mislabels whenever `n>50` (clamped) or fewer than `n` distinct values exist — e.g. `n=999` renders `999 of …`.
pr_info/steps/step_5.md:31 — medium — Deep-view block rendering is specified and tested only for string and numeric categories; temporal, boolean (`true/false/null`, `true %`), and other/binary (`size (bytes)` via DATALENGTH) blocks get no render spec here and no output assertion in step 5 or step 7 — the step-7 fixture has date and boolean columns but the listed assertions never check their rendered blocks, leaving boolean's distinct stat shape unverified.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_7.md around line 57, define the value-kind rule for the non_null == 0 case: an all-NULL (empty) column should be classified as value_kind == "none" rather than "sample", so it does not render the nonsensical \'sample values (1 of 0 distinct — every value unique)\'.', 'In pr_info/steps/step_7.md around line 60, make the sample header count reflect the actual shown count (len(values)) rather than the unclamped n: reconcile the n passed to render_summary/render_deep with the clamped_n used to build the value-list SQL (step_7.md:55) so headers do not mislabel when n>50 or when fewer than n distinct values exist.', "In pr_info/steps/step_5.md around line 31, add render specs for the deep-view temporal, boolean (true/false/null, true %), and other/binary (size bytes via DATALENGTH) categories, and add output assertions in step 5 or step 7 that check the rendered blocks for the date and boolean columns in the step-7 fixture, including boolean's distinct stat shape."], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-08-04
**Findings**:
Let me pin down the exact locations for the substantive issue I found around the empty-filter message.`pr_info/steps/step_7.md:49` — high — Pipeline step 6 computes only the *filtered* `build_count_sql` → `rows`; when a `where` predicate matches nothing, `rows` is 0, but `empty_filter_message(total_rows)` (step_6.md:28) requires the *unfiltered* table total to render the issue-specified `No rows match the where predicate (table has N rows)`. No unfiltered `COUNT(*)` is planned, so the message can only report `0 rows`, contradicting the issue example and defeating the empty-table-vs-empty-filter distinction. Plan needs an extra unfiltered count (or a different total source) in that branch.

`pr_info/steps/step_3.md:52` — low — Numeric `sum` falls back to `SUM(CAST(c AS FLOAT))` for all non-integer T-SQL numerics (decimal/money); the FLOAT cast is unnecessary (only integer types risk the overflow the BIGINT cast guards against) and lossy, so a money/decimal column's sum can be misreported — the "silently wrong number" failure mode the issue calls out for `AVG`. Cast only integers; leave decimal/money uncast on T-SQL.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_7.md around line 49, add an unfiltered COUNT(*) (or another unfiltered total source) for the empty-filter branch so empty_filter_message (step_6.md:28) can render 'No rows match the where predicate (table has N rows)' with the true table total instead of the filtered 0, preserving the empty-table-vs-empty-filter distinction.", 'In pr_info/steps/step_3.md around line 52, restrict the numeric sum CAST to integer types only: cast integers to BIGINT to guard overflow, but leave decimal/money columns uncast rather than using SUM(CAST(c AS FLOAT)), which is lossy and can misreport the sum.'], escalate_reason=None)
**Changes**:
applied

## Round 4 — 2026-08-04
**Findings**:
Now the step files:Let me verify key codebase assumptions the plan depends on.Let me pin exact line numbers for the findings.`pr_info/steps/step_4.md:37` — high — Sample-value SQL (`SELECT DISTINCT c … ORDER BY c`) does not exclude NULL, but `value_kind="sample"` is chosen whenever `distinct == non_null` (step_7.md:69). A unique-non-null column that also holds NULLs returns NULL as an extra DISTINCT row, so `len(values)` exceeds `distinct` (D) and the deep-view header renders `sample values (N of D distinct — every value unique)` with N>D — the same nonsensical class the plan guards only for the all-NULL case (step_7.md:65-68). Add `WHERE c IS NOT NULL` to the sample query (or derive the header count from non-null only).
`pr_info/steps/step_7.md:49` — high — The pipeline never states that the user `params` dict is passed to `execute_readonly_query` for `build_count_sql`, `build_scalar_sql`, and `build_value_list_sql`. Their re-rendered predicate carries `:name` placeholders (step_2.md), so any `where` using placeholders fails at execution unless `params` is threaded through all data-query executions. Only the metadata query (`{schema, table}`) and `validate_where` mention params.
`pr_info/steps/step_3.md:36` — low — `_other_exprs(idx, col_ref, dialect)` signature (3 params) contradicts the dispatch in the ALGORITHM (step_3.md:~55) which calls every builder uniformly with `(i, ref, m.declared_type, dialect, include_distinct)` (5 args).
`pr_info/steps/step_6.md:38` — low — Triage silently drops value min/max for string/boolean/other columns (renders `—`), narrowing the issue's triage line spec ("name, type, null %, distinct count, min/max"); this reduction is not listed in the issue's Out-of-scope section, only self-justified in the plan.
`pr_info/steps/step_7.md:105` — low — `tach.toml` `depends_on` for `summarize` lists `config`, but summary.md:59 omits it and the `count_tools` precedent (tach.toml:90-96) carries no `config` dependency (it is a TYPE_CHECKING-only import). Inconsistent; likely an unnecessary declared dependency.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_4.md around line 37, add WHERE c IS NOT NULL to the sample-value query (SELECT DISTINCT c ... ORDER BY c) — or derive the deep-view header count from non-null values only — so a unique-non-null column that also holds NULLs does not return NULL as an extra DISTINCT row and render 'sample values (N of D distinct — every value unique)' with N>D.", 'In pr_info/steps/step_7.md around line 49, specify that the user params dict is threaded through execute_readonly_query for build_count_sql, build_scalar_sql, and build_value_list_sql (not only the metadata and validate_where paths), so where predicates using :name placeholders execute instead of failing.', 'In pr_info/steps/step_3.md around line 36, reconcile the _other_exprs signature with the ALGORITHM dispatch (step_3.md:~55): every builder is called uniformly with (i, ref, m.declared_type, dialect, include_distinct), so _other_exprs must accept the same 5-argument signature rather than (idx, col_ref, dialect).', "In pr_info/steps/step_6.md around line 38, resolve the triage min/max reduction: the issue's triage line spec requires min/max for all columns, so either render value min/max for string/boolean/other columns or explicitly document this reduction as an accepted scope change rather than silently rendering '—'."], escalate_reason=None)
**Changes**:
applied
